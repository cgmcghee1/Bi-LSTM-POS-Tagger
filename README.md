# Bi-LSTM

A small experiment with TF and Keras to create a Bi-LSTM POS tagger. Need to work out a better way of handling unknown words and would like to create my own implementation of an LSTM cell, instead of using TF/Keras.

Note: this really doesn't work as intended and is a work in progress.
